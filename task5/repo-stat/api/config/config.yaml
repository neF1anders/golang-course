app:
  app_name: "repo-stat-api"

http:
  address: "localhost:8081"
  timeout: "5s"

logger:
  log_level: "DEBUG"

services:
  subscriber: "subscriber:8082"
  processor: "processor:8083"
  subscribe: "subscribe:8085"